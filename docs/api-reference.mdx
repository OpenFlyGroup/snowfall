import { Meta } from "@storybook/addon-docs/blocks";
import { SnowCanvas } from "../src/components/SnowCanvas";
import { ArgTypes } from "@storybook/addon-docs/blocks";

<Meta title="API Reference" />

# API Reference

## `SnowCanvas`

Основной компонент, который отрисовывает снег на всём экране или в заданной области.

### Props

<ArgTypes of={SnowCanvas} />

### Пример

```tsx
import { SnowCanvas } from "@openflygroup/snowfall";

function App() {
  return (
    <SnowCanvas
      accumulationElements={[".card", "h1", ".snow-surface"]}
      className="snow-overlay"
      style={{ opacity: 0.8 }}
      onSnowflakeStuck={(elementId, flake) => {
        console.log(`Snowflake stuck to ${elementId}:`, flake);
      }}
    />
  );
}
```

## Другие экспортируемые сущности

- `SnowAccumulation` — отрисовка накопившегося снега на конкретном элементе (`elementId`)
- `SnowControls` — панель управления интенсивностью, темами и накоплением
- `useSnow` — хук для программного управления конфигом снега
- `useSnowStore` — прямой доступ к Zustand-стоянию (для продвинутых сценариев)
- `presets` — готовые пресеты интенсивности и цветовых тем
- `createSnowConfig` — утилита для создания конфига с дефолтами
