import { Meta, Story } from "@storybook/addon-docs/blocks";
import { SnowCanvas } from "../src/components/SnowCanvas";

<Meta title="Introduction" />

# @openflygroup/snowfall

<Story name="Showcase">
  {() => {
    const style = {
      width: "100%",
      height: "400px",
      background: "linear-gradient(to bottom, #0f172a, #1e293b)",
      borderRadius: "12px",
      position: "relative",
      overflow: "hidden",
    };

    return (
      <div style={style}>
        <SnowCanvas />
        <div
          style={{
            position: "absolute",
            top: "50%",
            left: "50%",
            transform: "translate(-50%, -50%)",
            textAlign: "center",
            color: "white",
            zIndex: 100,
          }}
        >
          <h1 style={{ fontSize: "3rem", marginBottom: "1rem" }}>
            ❄️ Snowfall
          </h1>
          <p style={{ fontSize: "1.2rem", opacity: 0.9 }}>
            Beautiful, customizable snowfall effects for React
          </p>
        </div>
      </div>
    );
  }}
</Story>

## ✨ Возможности

- **🎨 Полная кастомизация** — размер, цвет, форма и скорость снежинок
- **🏔️ Накопление на элементах** — снег «налипает» на выбранные DOM-элементы
- **⏱️ Авто-затухание** — постепенное исчезновение снега по таймеру
- **📱 Высокая производительность** — отрисовка через canvas с плавной анимацией
- **🎯 TypeScript** — полностью типизированный API
- **🎮 Панель управления** — готовые контролы для изменения настроек на лету

## 🚀 Быстрый старт

```bash
npm install @openflygroup/snowfall zustand
```

```tsx
import {
  SnowCanvas,
  SnowAccumulation,
  SnowControls,
} from "@openflygroup/snowfall";

function WinterApp() {
  return (
    <>
      <SnowCanvas accumulationElements={[".card", "h1", "button"]} />
      <SnowAccumulation elementId="header" />
      <SnowControls />

      <h1 className="relative">Winter Wonderland</h1>
      <div className="card relative">Snow will accumulate here</div>
    </>
  );
}
```

## 📚 Далее

- Откройте раздел **API Reference** для подробного описания пропсов
- Посмотрите готовые истории компонентов `SnowCanvas`, `SnowControls` и `SnowAccumulation`
- Зайдите на сайт-демо чтобы увидеть библиотеку в действии
